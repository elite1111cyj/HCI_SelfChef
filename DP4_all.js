//[START] JIHYEONG
var now_ID
function get_id(typed_id){
    if (typed_id){
        now_ID=typed_id
    }
    else {now_ID=typed_id}

}
var test=1;
 //ID of the current user
var search_key="default_key"; // search keyword typed in. string ex) "carrot" if present, null if not present
var search_category="default_category"; //category chosen. string ex) "vegetable", "fruit" if present, null if not present

//[END] JIHYEONG